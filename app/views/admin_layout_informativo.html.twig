{% extends 'layout.html.twig' %}

{% block secondaryNavigation %}
     <div class="container">
        <ul class="nav nav-tabs">
            <li {% if 'adicionar-informativo' == active %}class="active"{% endif %}><a href="{{ path('adicionar-informativo')}}">Adicionar</a></li>
            <li {% if 'listar-informativo' == active %}class="active"{% endif %}><a href="{{ path('listar-informativo')}}">Listar</a></li>            
          </ul>
    </div> 
{% endblock %}
