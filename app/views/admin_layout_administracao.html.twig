{% extends 'layout.html.twig' %}

{% block secondaryNavigation %}
     <div class="container">
        <ul class="nav nav-tabs">
            <li {% if 'adicionar-usuario' == active %}class="active"{% endif %}><a href="{{ path('adicionar-usuario')}}">Adicionar</a></li>
            <li {% if 'admin-administracao' == active %}class="active"{% endif %}><a href="{{ path('admin-administracao')}}">Listar</a></li>            
          </ul>
    </div> 
{% endblock %}
